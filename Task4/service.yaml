apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: nginx
  ports:
  - name: port80
    protocol: TCP
    port: 80
    targetPort: 80
  - name: port50
    protocol: TCP
    port: 50
    targetPort: 50
  type: ClusterIP      